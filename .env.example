# LaunchDarkly Configuration
# Get your credentials from: https://app.launchdarkly.com
REACT_APP_LD_CLIENT_SIDE_ID=your-client-side-id-here
LAUNCHDARKLY_SDK_KEY=your-server-sdk-key-here

# OpenAI Configuration (for AI Chatbot feature)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Server Configuration
PORT=3001

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Get your LaunchDarkly Client-side ID from: https://app.launchdarkly.com/settings/projects
#    - Go to your project settings
#    - Select your environment (Test/Production)
#    - Copy the "Client-side ID" for REACT_APP_LD_CLIENT_SIDE_ID
# 3. Get your LaunchDarkly Server SDK key from the same location
#    - Copy the "SDK key" for LAUNCHDARKLY_SDK_KEY
# 4. Get your OpenAI API key from: https://platform.openai.com/api-keys
#    - Create a new API key if you don't have one
# 5. Replace the placeholder values above with your actual keys

# Required LaunchDarkly Feature Flags:
# Create these flags in your LaunchDarkly dashboard:
# - advanced-analytics (Boolean flag for F1 - Advanced Analytics)
# - f2-ai-chatbot (Boolean flag for F2 - AI Chatbot with targeting rules)
# - chatbot-config (AI Config for automatic model assignment)

# User Targeting Setup:
# Set up targeting rules in LaunchDarkly for f2-ai-chatbot:
# - Target users with role = "lab-owner" → ON
# - Target users with role = "beta-user" → ON
# - Target users with role = "standard-user" → OFF